{
  "name": "intern-auth-monorepo",
  "version": "1.0.0",
  "description": "Master package for the auth project.",
  "scripts": {
    "install-all": "(cd backend && npm install) && (cd frontend && npm install)",
    "dev": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
